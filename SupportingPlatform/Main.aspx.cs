using System;
using Nature.BaseWebform;

namespace NatureFramework.SupportingPlatform
{
    public partial class Main : PageUserInfo
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            //Response.Write("<br>UserID:" + MyUser.UserID);
            //Response.Write("<br>RoleIDs:" + MyUser.RoleIDs);
            //Response.Write("<br>ModuleIDs:" + MyUser.ModuleIDs);

        }
    }
}